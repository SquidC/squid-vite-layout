/**
 * markdown 解析器 输入文件路径 解释成demo-vue组件 生成浏览器可执行js
 * @param raw 编译出来的tsx配置
 * @param path 文件路径
 * @returns 返回处理后的文件内容
 */
export function layoutCompiler(raw: string, path: string): string {
  // 编译 @squidc/element/layout

  // 然后替换config为raw
  
  return ""
}
